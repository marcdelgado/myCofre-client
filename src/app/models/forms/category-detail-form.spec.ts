import { CategoryDetailForm } from './category-detail-form';

describe('CategoryDetailForm', () => {
  it('should create an instance', () => {
    expect(new CategoryDetailForm()).toBeTruthy();
  });
});
