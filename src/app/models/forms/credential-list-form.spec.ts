import { CredentialListForm } from './credential-list-form';

describe('CredentialListForm', () => {
  it('should create an instance', () => {
    expect(new CredentialListForm()).toBeTruthy();
  });
});
